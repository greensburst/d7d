import 'package:flutter/material.dart';
import '../view/app_bar.dart';

class Explore extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: Appbar(title: "发现"),
    );
  }
}